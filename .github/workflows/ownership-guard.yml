name: Ownership guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify-authors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Verify commit authors
        run: python3 scripts/verify_ownership.py
